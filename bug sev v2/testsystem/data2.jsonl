{"project_name": "example_project", "project_version": "v1.0.0", "label": 2, "code": "public class DataProcessor {\n    private List<Data> dataList;\n\n    public DataProcessor(List<Data> dataList) {\n        this.dataList = dataList;\n    }\n\n    public void processData() {\n        if (dataList == null || dataList.isEmpty()) {\n            return;\n        }\n        for (Data data : dataList) {\n            if (data.isValid()) {\n                process(data);\n            }\n        }\n    }\n\n    private void process(Data data) {\n        // Processing logic\n    }\n}", "code_comment": "public class DataProcessor", "code_no_comment": "public class DataProcessor { private List<Data> dataList; public DataProcessor(List<Data> dataList) { this.dataList = dataList; } public void processData() { if (dataList == null || dataList.isEmpty()) { return; } for (Data data : dataList) { if (data.isValid()) { process(data); } } } private void process(Data data) { } }", "lc": 20, "pi": 0.5, "ma": 0.4, "nbd": 4, "ml": 5, "d": 0.7, "mi": 0.3, "fo": 0.5, "r": 0.2, "e": 0.8}
